Add a Bridge Memory entry for Vibe Academy covering database structure and migration practices.

Required sections:
  - Schema overview (tables, key relationships, ER summary)
  - Naming conventions (tables, columns, indexes, FKs)
  - Migration tool + workflow (how to create, run, rollback)
  - Best practices we follow (transactions, idempotency, zero-downtime patterns, seed data handling)
  - Anti-patterns to avoid (with examples from this repo)

Tag the entry: database, schema, migrations, conventions
Link it to the existing ARCHITECTURE.md and CONVENTIONS.md memory entries.

Done = entry created + tagged + linked + visible in graph.