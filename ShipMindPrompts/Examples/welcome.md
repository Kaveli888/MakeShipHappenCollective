# Welcome to ShipMind Prompts

This is your personal prompt library for ShipMind. Drop `.md` or `.txt` files into `~/MakeShipHappenCollective/ShipMindPrompts/<folder>/` and they'll appear here.

The first line (or `# heading`) becomes the title. Everything else is the prompt body.
