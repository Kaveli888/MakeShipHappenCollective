Review my Codex config and turn on Yolo / auto-approve mode so it never prompts me for permission.

Steps:
  1. Locate the Codex config file (~/.codex/config.* or equivalent).
  2. Show me current settings before changing.
  3. Set the auto-approve / no-confirm flag.
  4. Verify by showing the diff.
  5. Confirm with a dry-run command that it doesn't prompt.

Heads-up note for me: list the 3 highest-risk things Yolo mode now lets it do without asking, so I'm aware.

Done = setting enabled + diff shown + risk note delivered.