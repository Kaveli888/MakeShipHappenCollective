A viewer suggested adding YAML front matter to memory markdown files for housekeeping.

Do an in-depth review:
  1. Restate what they're proposing in 2-3 sentences.
  2. List concrete benefits (search, filter, graph metadata, dataview-style queries, Obsidian compat).
  3. List concrete costs (migration, schema lock-in, parser overhead, UX surface).
  4. Propose a minimal viable schema (3-5 fields) and a maximal schema.
  5. Recommend: ship / defer / decline + why.

Done = structured review delivered. No code changes.