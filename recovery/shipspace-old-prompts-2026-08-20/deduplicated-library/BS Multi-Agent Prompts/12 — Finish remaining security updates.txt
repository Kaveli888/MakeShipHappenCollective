Continue with any unfinished items from the security audit.

Rules:
  - Critical and High severity only in this pass.
  - Each fix in its own commit with the finding ID in the message.
  - Re-test the affected endpoint after each fix.

When every Critical and High is closed, post the literal word "done" on its own line as your final output.

Done = all Critical + High closed + "done" posted.