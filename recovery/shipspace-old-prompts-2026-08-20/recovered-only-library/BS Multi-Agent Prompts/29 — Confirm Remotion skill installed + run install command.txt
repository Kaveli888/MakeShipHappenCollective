Confirm the Remotion skill is installed. Then run the install command.

Steps:
  1. Verify presence of the skill in the skills directory.
  2. Show me the exact command you'd run.
  3. Run it.
  4. Show full output.
  5. Verify post-install state.

If anything fails, stop and surface the error — do not retry blindly.

Done = command run + output shown + install verified.