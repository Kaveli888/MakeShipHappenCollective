# Cloud config for the desktop app (Phase 2). Leave unset to stay local-first.
# The ANON key is safe in the client (gated by RLS). NEVER put the service-role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
