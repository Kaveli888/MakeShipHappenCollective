{
  "name": "ship-memory",
  "version": "0.1.0",
  "private": true,
  "description": "Ship Memory — a standalone, portable markdown-vault memory engine. Embeds in ShipSpace, plugs into any app, connects to third-party sources.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @ship-memory/core && npm run build --workspace @ship-memory/cli && npm run build --workspace @ship-memory/mcp && npm run build --workspace @ship-memory/connector-obsidian",
    "test": "npm run build && node --test tests/*.test.mjs",
    "dev": "npm run dev --workspace @ship-memory/core",
    "mcp": "node packages/mcp/dist/index.js",
    "app": "npm run dev --workspace @ship-memory/app",
    "app:build": "npm run build --workspace @ship-memory/app",
    "shipmemory": "npm run app",
    "shipmemory:dev": "npm run dev",
    "shipmemory:app": "npm run app",
    "shipmemory:mcp": "npm run mcp",
    "shipmemory:cli": "node packages/cli/dist/index.js"
  }
}
